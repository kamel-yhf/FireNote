import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.firenote',
  appName: 'firenote',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
